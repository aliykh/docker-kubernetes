version: "v3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:5001"
    environment:
      ENVIRONMENT: "local"
    volumes:
      - .:/app
    depends_on:
      - redis-server
  redis-server:
    build:
      dockerfile: redis.Dockerfile
